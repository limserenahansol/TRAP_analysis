Steps 1–3 publication-style schematics (PNG)
============================================

PNG files (open directly in any viewer):
  fig01_overall_flow.png
  fig02_region_sets.png
  fig03_bias_multiplicity.png

Regenerate (Python 3 + matplotlib):
  cd to this folder
  python render_png.py

PHASE_LABELS_AND_Z.txt — what “phase normalization” means in this repo
  (canonical phase labels at load vs optional within-phase z-scoring).

Repository: https://github.com/limserenahansol/TRAP_analysis
