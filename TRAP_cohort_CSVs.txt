# One TRAP density export per cohort (same Allen atlas ids / row order in every file).
# Single combined workbook: all manifest rows load from this file; cohort_id is for sample labels (C1__, C2__) only.
Hansol Lim 561 cell counts + density.xlsx
