# Rubric items — Neuro-Agent TRAP Step 13 task

## Critical scientific reasoning (must address)

1. **Sample inclusion** — States that analysis uses **20 mice** from the manifest (`include=1`), not all 21 spreadsheet columns; names **`HaLi_020326_11`** as excluded.

2. **Both cohorts** — Acknowledges **two cohorts** (Oct 2025 + Feb–Mar 2026) pooled via one manifest; does not describe the dataset as “8 mice only.”

3. **Density definition** — Identifies quantification as **TRAP-positive cell density** using **calculated_mm3** (cells per mm³ of **sample tissue volume**), not raw fluorescence or Allen reference volume alone.

4. **Z-scoring scope** — Explains that **`z_within_phase`** z-scores each region **within a phase across mice**, then aggregates by group — not global z-score across all phases pooled.

5. **Region filter** — States that regions are filtered by **Step 3 hierarchy567** first, then **forebrain / no brainstem** (and fiber stripping in the full pipeline); does not confuse with the full ~839-row Allen export.

6. **Cluster labels** — Uses **Step 3 universal K=4 labels** as the primary partition; if re-running k-means for sanity checks, distinguishes that from the **fixed** published labels.

7. **K=4 justification** — Does **not** claim “silhouette is highest at k=4” unless true for the shown mask; uses **elbow / variance explained**, **interpretability**, and **consistency with Step 3** appropriately.

8. **Active vs Passive** — Compares groups using **manifest delivery** labels on **mouse-level** values; does not collapse to one mean per group without stating n.

9. **Cluster 1 biology (example)** — If discussing Cluster 1, notes that many regions show **Active > Passive in Post / Reinstatement** but not necessarily all phases — consistent with direction heatmap sorting.

10. **Hard assignment** — Correctly states each region has **one** cluster ID; overlapping PCA clouds ≠ dual membership.

## Expected deliverables

- [ ] PCA / cluster map figure or equivalent table (`PC1`, `PC2`, cluster ID)
- [ ] K-sanity summary (silhouette and/or variance explained vs k)
- [ ] Cluster trajectory or phase × group summary table
- [ ] Cluster 1 (or chosen cluster) Active−Passive direction summary
- [ ] Short methods paragraph suitable for a PI / non-expert reader

## Common errors (penalize)

- Using only **cohort 1** (8 mice) when 20 are available
- Using **`cells/mm^3` Allen tree** instead of **`calculated_mm3`**
- Ranking “top regions” from the **full roster CSV** without stating ranking metric (silhouette vs PC1 vs mean density)
- Confusing **716 whole-brain-no-fiber** count with **~286 Step 3** region count
- Claiming **>80% variance explained** from elbow plot without defining TSS/WSS

## Reference outputs

Compare agent outputs to:

- `outputs/reference_matlab_step13/` (copied from MATLAB run)
- `outputs/solver_run/` (generated by `python solver.py` after `python build_package.py`)
